## Process this file with automake to produce Makefile.in

dxbindir = $(prefix)/dx/bin
bindir = $(prefix)/bin

EXTRA_DIST = dxworker dx mdf2c url_handler.sh

dxbin_SCRIPTS = dxworker mdf2c dx url_handler.sh
bin_SCRIPTS = dx
